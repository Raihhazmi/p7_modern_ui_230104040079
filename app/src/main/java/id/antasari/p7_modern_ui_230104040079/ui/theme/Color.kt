package id.antasari.p7_modern_ui_230104040079.ui.theme

import androidx.compose.ui.graphics.Color

// Warna Referensi (Domino's Style adaptation)
val RedPrimary = Color(0xFFFF5252) // Merah utama
val RedDark = Color(0xFFC62828)
val PinkSoft = Color(0xFFFDECEC) // Untuk background TextField
val WhitePure = Color(0xFFFFFFFF)
val BlackText = Color(0xFF1D1B20)
val GreyText = Color(0xFF757575)

// Warna Dark Mode
val DarkBackground = Color(0xFF121212)
val DarkSurface = Color(0xFF1E1E1E)
val RedPrimaryDark = Color(0xFFFF8A80) // Merah lebih terang untuk dark mode

val PrimaryLight = RedPrimary
val OnPrimaryLight = WhitePure
val ContainerLight = PinkSoft

val PrimaryDark = RedPrimaryDark
val OnPrimaryDark = DarkBackground
val ContainerDark = DarkSurface